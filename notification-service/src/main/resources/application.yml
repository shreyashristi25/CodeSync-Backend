spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:mysql://localhost:3306/notification_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ${NOTIFICATION_DB_PASSWORD:Shreya@2002}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mvc:
    cors:
      allowed-origin-patterns: "http://localhost:4200"
      allowed-methods: "*"
      allowed-headers: "*"
      allow-credentials: true

eureka:
  instance:
    prefer-ip-address: true

app:
  services:
    auth: http://localhost:8081
  email:
    from: ${EMAIL_FROM:onboarding@resend.dev}
    api-key: ${RESEND_API_KEY:re_KRT8bePo_Nj2T8FqqNix8bfNESFhXvwow}
    base-url: ${FRONTEND_URL:http://localhost:4200}

server:
  port: 8088

logging:
  level:
    root: INFO
    com.codesync: DEBUG